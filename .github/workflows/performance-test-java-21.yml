name: Daily Performance Test Java 21

on:
  schedule:
    - cron: '0 2 * * *'

permissions:
  contents: write
  pull-requests: write

jobs:
  performance-test:
    uses: ./.github/workflows/performance-test-base.yml
    with:
      javaVersion: "21"