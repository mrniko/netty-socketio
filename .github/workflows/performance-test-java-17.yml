name: Daily Performance Test Java 17

on:
  schedule:
    - cron: '0 1 * * *'

permissions:
  contents: write
  pull-requests: write

jobs:
  performance-test:
    uses: ./.github/workflows/performance-test-base.yml
    with:
      javaVersion: "17"